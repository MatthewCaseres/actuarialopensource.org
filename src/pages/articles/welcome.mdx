import SidebarLayout from '@/components/Sidebar/SidebarLayout'
import Image from 'next/future/image'

export default (props) => <SidebarLayout {...props} />

# Welcome

This is where we do our best to produce open source educational materials.

You can collapse/expand the sidebar with the button in the bottom left.
